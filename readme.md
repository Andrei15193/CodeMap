Before starting development/testing locally, ensure you have installed Jekyll: https://jekyllrb.com

For local development and testing, run the `install gems` task before running the `watch jekyll` task.